version: '3.8'

services:
  strategy-backtest:
    build:
      context: .
      dockerfile: Dockerfile
    image: strategy-backtest-platform:latest
    container_name: strategy-backtest-app
    ports:
      - "3000:3000"
    restart: unless-stopped
